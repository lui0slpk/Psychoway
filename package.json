{
  "name": "psychoway",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "frontend": "pnpm --dir Frontend start",
    "server": "pnpm --dir Backend start",
    "start": "concurrently \"pnpm run frontend\" \"pnpm run server\"",
    "build": "pnpm --dir Frontend build",
    "test": "pnpm --dir Frontend test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}